const text = 'holaa holaa sdfsda sadfsda prueba asa'

export function textEffect() {
    return myPromise = new Promise((resolve, reject) => {
        for (let i = 0; i < text.length; i++) {
            setInterval(() => {
                return text[i]
            }, 20)
        }
        resolve()
    })



}